---
export interface Props {
  title: string
  category?: string | undefined
  outline?: any
}

const { title, category } = Astro.props
---

<div title={category} transition:name='title' class='cardrelated outline'>
  {title.toLocaleUpperCase()}
</div>

<style lang='scss'>
  .cardrelated {
    display: fit-content;
    width: fit-content;
    color: var(--CIKOprimary);
    &.outline {
      padding: 0.25rem;
      border: 2px solid transparent;
      width: auto;
    }
  }
</style>
